node_modules/
# dist/ is committed so this fork can be consumed as a git dependency without a build step
*.log
.DS_Store
*.tsbuildinfo
dev/showcase.*
dev/multiline.*

# Secrets / local agent config
.env
.env.*
!.env.example
.claude/settings.local.json
